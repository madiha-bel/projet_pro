{% extends 'base.html.twig' %}

{% block title %}Options{% endblock %}

{% block body %}
    <h1>Options</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ option.id }}</td>
            </tr>
            <tr>
                <th>Reponse1</th>
                <td>{{ option.reponse1 }}</td>
            </tr>
            <tr>
                <th>Reponse2</th>
                <td>{{ option.reponse2 }}</td>
            </tr>
            <tr>
                <th>Reponse3</th>
                <td>{{ option.reponse3 }}</td>
            </tr>
            <tr>
                <th>Reponse4</th>
                <td>{{ option.reponse4 }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_options_index') }}">back to list</a>

    <a href="{{ path('app_options_edit', {'id': option.id}) }}">edit</a>

    {{ include('options/_delete_form.html.twig') }}
{% endblock %}
