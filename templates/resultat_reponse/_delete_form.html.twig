<form method="post" action="{{ path('app_resultat_reponse_delete', {'id': resultat_reponse.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ resultat_reponse.id) }}">
    <button class="btn">Delete</button>
</form>
