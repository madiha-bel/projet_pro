{% extends 'base.html.twig' %}

{% block title %}ResultatReponse{% endblock %}

{% block body %}
    <h1>ResultatReponse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ resultat_reponse.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_resultat_reponse_index') }}">back to list</a>

    <a href="{{ path('app_resultat_reponse_edit', {'id': resultat_reponse.id}) }}">edit</a>

    {{ include('resultat_reponse/_delete_form.html.twig') }}
{% endblock %}
